---
title: 维护发布
description: 本文介绍组件修改、版本管理、自动发布和网站依赖升级流程，以及发布失败后的处理方法。
summary: 共享组件在 ui 仓库维护，修改后提升包版本并更新变更记录，完成检查后推送 main。GitHub Actions 先验证，再通过 npm Trusted Publishing 发布尚未存在的稳定版本；同版本提交只检查，不覆盖已发布包。三个网站各自锁定依赖，发布后需要更新清单和锁文件、验证并部署。发布连接绑定仓库与工作流名称，变更这些名称时需要同步调整 npm 设置。
---

## 参考资料

- [发布流程 - GitHub](https://github.com/lailai0916/ui/blob/main/docs/releasing.md)
- [CI 工作流 - GitHub](https://github.com/lailai0916/ui/actions/workflows/ci.yml)
- [Trusted Publishing - npm](https://docs.npmjs.com/trusted-publishers/)

## 修改范围

| 改动                                        | 维护位置                  |
| ------------------------------------------- | ------------------------- |
| 组件行为、共有外观、主题变量、类型声明      | `lailai0916/ui`           |
| Home 的 Docusaurus 适配、文章组件与页面内容 | `lailai0916.github.io`    |
| Tools 的工具逻辑与工具页面                  | `tools`                   |
| Academy 的课程、学习业务与接口              | `academy`                 |
| 这组使用文档与组件预览                      | Home 的 `docs/project/ui` |

修改公开属性、默认值或导出时，同步更新使用文档、示例和迁移说明。先确认现有组件与变量能否完成需求，再决定新增接口。

## 发布版本

在组件库仓库中运行：

```bash
npm ci
npm version patch --no-git-tag-version
npm run check
npm pack --dry-run
```

`npm version` 同时更新 `package.json` 与锁文件。兼容修复使用补丁版本；1.0 之前的不兼容修改提升次版本，并在 `CHANGELOG.md` 记录影响与迁移步骤。

`npm run check` 执行格式、Lint、类型、库构建、测试与示例构建。还应使用 `npm pack` 生成实际安装包，在使用方验证构建和界面。发布内容为 `dist/`、包元数据、README 与许可证，不应夹带项目私有配置。

将发布改动提交并推送到 `main` 后，等待工作流的 `validate` 与 `publish` 均成功，再检查：

```bash
npm view @lailai0916/ui version
npm view @lailai0916/ui repository.url
```

确认版本与源码对应后，创建匹配的版本标签与 GitHub Release。标签用于追踪发布，当前自动发布由 `main` 提交触发。

## 自动发布规则

| 操作                                         | 结果                         |
| -------------------------------------------- | ---------------------------- |
| 推送 `main`，版本尚未发布，检查通过          | 自动发布到 npm               |
| 推送 `main`，版本已在 npm 存在               | 跳过发布                     |
| 修改代码但不提升版本                         | 不会覆盖 npm 中的旧代码      |
| 提交 PR 或推送标签                           | 只验证，不发布               |
| 在 `main` 手动运行 `CI`                      | 验证后按同一规则决定是否发布 |
| 检查失败、注册表异常或待发布版本低于最新版本 | 停止发布并报告失败           |

当前流程接受稳定的 `X.Y.Z` 版本。预发布版本需要另行设计 dist-tag 规则。已发布版本不能覆盖，修复后发布新版本。

## 更新网站

发布成功后，各网站仍保持原依赖版本。Home 和 Tools 在各自根目录运行：

```bash
npm install --save-exact @lailai0916/ui@0.2.0
npm run check
npm run build
```

Academy 使用工作区依赖：

```bash
npm install --save-exact @lailai0916/ui@0.2.0 -w @lailai/academy-web
npm run check
npm run build
```

把示例中的 `0.2.0` 替换为实际要升级的已发布版本。提交各网站的依赖清单和锁文件，再由网站部署流程上线。检查浅色、深色、系统主题、窄屏布局、表单和键盘操作；涉及默认值变化时按[迁移指南](/docs/project/ui/migration)逐项处理。

## 发布连接

npm 的 Trusted Publisher 应与实际仓库和工作流保持一致：

| 字段                 | 值             |
| -------------------- | -------------- |
| Provider             | GitHub Actions |
| Organization or User | `lailai0916`   |
| Repository           | `ui`           |
| Workflow Filename    | `ci.yml`       |
| Environment Name     | 留空           |
| Allow npm Publish    | 启用           |

CI 使用 GitHub 托管运行器、Node.js 24 和 OIDC 临时发布凭据，不需要保存 `NPM_TOKEN`。正常发布无需打开 npm 网站；仓库重命名、工作流文件改名或发布权限变化时，仍需更新该连接，npm 可能要求账号安全验证。

## 故障处理

先查看失败的工作流步骤。验证失败时修复源码；身份验证失败时核对仓库、工作流和 npm 发布连接；注册表异常时确认版本状态后再重跑。已存在的版本会被跳过，不能靠重跑覆盖。

网站出现回归时，可以把该网站依赖恢复到上一个已验证版本并重新部署，同时在组件库发布修复版本。只回退 Git 提交而不核对锁文件与部署结果，不能确认线上已恢复。
