---
title: lailai-template
description: 本文介绍 lailai-template 项目，一个开箱自带 Claude 配置与 lailai-skill 子模块的通用 GitHub 起步模板。
summary: lailai 的通用 GitHub 起步模板。点 Use this template 即可新建仓库，开箱带上 .claude 配置、以子模块引入的 lailai-skill 分身、issue 与 PR 模板，以及 editorconfig、gitattributes、gitignore 等编辑器与 git 卫生文件。设计上不复制任何风格规则，而是把 lailai-skill 作为单一来源以子模块接入，由 CLAUDE.md 统一 defer 过去，各仓库只维护自己的项目专属规则层。页面附上 GitHub 仓库卡片，可从这里进入查看目录结构与使用方式。
---

## 项目仓库

<GitHub repo="lailai0916/lailai-template" />

## 使用

点仓库页的 **Use this template** 新建仓库，克隆后初始化分身，再改写 README：

```bash
git submodule update --init
```
