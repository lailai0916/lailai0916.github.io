# 安装

## 安装

打开终端并输入：

```bash
npx create-docusaurus@latest my-website classic --typescript
```

## 运行

切换到项目目录：

```bash
cd my-website
```

启动本地服务器：

```bash
npm start
```

等待一段时间，会自动在浏览器打开 `http://localhost:3000`。

此时你能看到 Docusaurus 网站的默认页面。

## 项目结构

- `my-website` 目录的默认结构如下：

```plaintext
my-website
├── blog
├── docs
├── src
├── docusaurus.config.js
├── package.json
└── README.md
```

- `blog`：博客文章目录。
- `docs`：文档目录。
- `src`：网站源代码目录。
- `docusaurus.config.js`：Docusaurus 配置文件。
- `package.json`：项目依赖和脚本配置文件。
- `README.md`：项目说明文件。
