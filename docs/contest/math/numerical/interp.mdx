---
title: 插值
description: 本文将介绍拉格朗日插值，用基函数构造过已知点的多项式，并给出模意义下求单点值的实现。
summary: 拉格朗日插值由 n 个横坐标互不相同的点唯一确定一个次数不超过 n-1 的多项式。做法是为每个点构造一个在自身处取 1、在其余点处取 0 的基函数，乘以该点纵坐标后求和，即得经过全部点的插值多项式，可直接代入求任意单点的取值。给出插值公式与模意义下的实现，其中除法通过快速幂求逆元完成，求单点值的时间复杂度为 O(n^2)。配套例题为洛谷 P4781。
---

## 参考资料

- [插值 - OI Wiki](https://oi-wiki.org/math/numerical/interp/)

## 拉格朗日插值

由 $n$ 个横坐标互不相同的点 $(x_i,y_i)$ 可唯一确定一个不超过 $n-1$ 次的多项式 $f$。拉格朗日插值为每个点构造一个在 $x_i$ 处取 $1$、在其余 $x_j$ 处取 $0$ 的基函数，乘以 $y_i$ 后求和即得 $f$，可直接代入求任意点的值。求单点值的时间复杂度为 $O(n^2)$。

$$
f(x)=\sum_{i=1}^ny_i\cdot\prod_{j\ne i}\frac{x-x_j}{x_i-x_j}
$$

```cpp
ll lagrange(int n,int k)
{
	ll ans=0;
	for(int i=1;i<=n;i++)
	{
		ll p=1,q=1;
		for(int j=1;j<=n;j++)
		{
			if(i==j)continue;
			p=p*(k-x[j])%mod;
			q=q*(x[i]-x[j])%mod;
		}
		ans=(ans+y[i]*(p*Pow(q,mod-2)%mod)%mod)%mod;
	}
	return (ans+mod)%mod;
}
```

## 例题

<Problem id="P4781" />
