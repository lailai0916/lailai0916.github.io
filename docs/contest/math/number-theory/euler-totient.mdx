---
title: 欧拉函数
description: 本文将介绍欧拉函数的定义与积性连乘公式，并给出欧拉定理与扩展欧拉定理的降幂公式。
summary: 欧拉函数 φ(n) 表示不超过 n 且与 n 互质的正整数个数，φ(1)=1，n 为质数时取 n−1。它是积性函数，由质因数分解可得连乘积公式，即 n 乘以每个质因子的 1 减 p 分之一。按此式枚举质因子累乘，单次求值时间复杂度为 O(根号 n)。与之紧密相关的欧拉定理指出：当 a 与 m 互质时，a 的 φ(m) 次方模 m 等于 1；扩展欧拉定理进一步给出 a、m 一般情形下的降幂公式，按 b 与 φ(m) 的大小分三种情况处理 a 的 b 次方取模。
---

## 参考资料

- [欧拉函数 - OI Wiki](https://oi-wiki.org/math/number-theory/euler-totient/)

## 定义

欧拉函数（Euler's totient function）$\varphi(n)$ 表示不超过 $n$ 且与 $n$ 互质的正整数个数。$\varphi(1)=1$；$n$ 为质数时 $\varphi(n)=n-1$。

它是积性函数。设 $n$ 的质因数分解为 $n=\prod p_i^{k_i}$，则

$$
\varphi(n)=n\prod_{p\mid n}\left(1-\frac1p\right)
$$

## 实现

按上式枚举 $n$ 的质因子并累乘，时间复杂度为 $O(\sqrt n)$。

```cpp
ll phi(ll n)
{
	ll res=n;
	for(ll i=2;i*i<=n;i++)
	{
		if(n%i==0)
		{
			res=res/i*(i-1);
			while(n%i==0)n/=i;
		}
	}
	if(n>1)res=res/n*(n-1);
	return res;
}
```

## 欧拉定理

与欧拉函数紧密相关的一个定理就是欧拉定理。其描述如下：

若 $\gcd(a,m)=1$，则 $a^{\varphi(m)}\equiv 1\pmod{m}$。

### 扩展欧拉定理

当然也有扩展欧拉定理，用于处理一般的 $a$ 和 $m$ 的情形。

$$
a^b\equiv
\begin{cases}
  a^{b\bmod\varphi(m)} & \gcd(a,m)=1 \\
  a^b & \gcd(a,m)\ne 1,b<\varphi(m) \\
  a^{b\bmod\varphi(m)+\varphi(m)} & \gcd(a,m)\ne 1,b\ge\varphi(m)
\end{cases}
\pmod m
$$

## 例题

<Problem id="P5091" />
