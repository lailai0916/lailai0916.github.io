---
title: 回文树
summary: 回文树（回文自动机，PAM）的每个节点恰好对应原串的一个本质不同回文子串，fail 指针指向该回文串的最长回文真后缀，并设偶根、奇根两个初始节点。它逐字符在线构建，总状态数不超过串长，构建复杂度为线性。借助节点与 fail 树结构，回文树能统计本质不同回文串的个数、每个回文串的出现次数等回文相关问题。
---

## 参考资料

- [回文树 - OI Wiki](https://oi-wiki.org/string/pam/)

## 简介

**回文树**（回文自动机，Palindromic Automaton，PAM）的每个节点恰好对应原串的一个本质不同的回文子串，$\operatorname{fail}$ 指针指向该回文串的最长回文真后缀。它有偶根、奇根两个初始节点。

逐字符在线构建，总状态数为本质不同回文子串个数（不超过 $n$），构建复杂度 $O(n)$。它能统计本质不同回文串个数、每个回文串的出现次数等。

## 例题

<Problem id="P5496" />
