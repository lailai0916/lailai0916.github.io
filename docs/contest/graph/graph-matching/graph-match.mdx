---
title: 图匹配
summary: 梳理三类图匹配。二分图最大匹配用匈牙利算法，依次为左部每个点寻找增广路——交替经过非匹配边与匹配边、两端均为非匹配点的路径，翻转其上匹配状态即让匹配数加一，复杂度 O(nm)。二分图最大权匹配用 KM 算法，给两部点各设顶标，只在满足顶标和等于边权的相等子图上找完美匹配，找不到增广路时调整顶标，用 slack 数组优化到 O(n^3)。一般图最大匹配用带花树算法，搜索时把遇到的奇环缩成一点绕开，复杂度 O(n^3)。
---

## 参考资料

- [图匹配 - OI Wiki](https://oi-wiki.org/graph/graph-matching/graph-match/)
- [二分图最大匹配 - OI Wiki](https://oi-wiki.org/graph/graph-matching/bigraph-match/)
- [二分图最大权匹配 - OI Wiki](https://oi-wiki.org/graph/graph-matching/bigraph-weight-match/)
- [一般图最大匹配 - OI Wiki](https://oi-wiki.org/graph/graph-matching/general-match/)

## 二分图最大匹配

匈牙利算法依次为左部每个点寻找匹配。从一个点出发找 **增广路**——交替经过非匹配边与匹配边、两端均为非匹配点的路径，翻转其上的匹配状态即可让匹配数加一。`match(u)` 递归地为 $u$ 腾出一个可用的右部点。时间复杂度为 $O(nm)$。

```cpp
bool match(int u)
{
	for(auto v:G[u])
	{
		if(vis[v])continue;
		vis[v]=1;
		if(!a[v]||match(a[v]))
		{
			a[v]=u;
			return 1;
		}
	}
	return 0;
}
```

## 二分图最大权匹配

**二分图最大权匹配** 用 **KM 算法** 求解。它给左右部每个点各一个 **顶标** $l_x,l_y$，只在满足 $l_x(u)+l_y(v)=w(u,v)$ 的 **相等子图** 上寻找完美匹配。

当相等子图上找不到增广路时，按当前交错树调整顶标，使更多边进入相等子图，直到匹配完美。朴素实现 $O(n^4)$，用 slack 数组优化后为 $O(n^3)$。

## 一般图最大匹配

一般图（不一定是二分图）的最大匹配用 **带花树**（Blossom）算法求解。它在 BFS 寻找增广路时，一旦遇到奇环（花）就把整朵花缩成一个点继续搜索，从而绕开奇环对增广的阻碍。复杂度 $O(n^3)$。

## 例题

<Problem id="P3386" />

<Problem id="P10937" />

<Problem id="P6577" />

<Problem id="P6113" />
