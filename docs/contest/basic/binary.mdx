---
title: 二分
summary: 二分（Binary Search）借助答案的单调性，每次将区间折半逼近边界，复杂度 O(logn)。约定 check 函数的返回序列形如若干 0 接若干 1，循环结束后 l 即第一个 1 的位置、l-1 为最后一个 0 的位置，据此给出整数与实数两套写法。三分（Ternary Search）处理单峰或单谷函数的极值，每轮取区间的两个三等分点，比较两处函数值排除不含极值的一侧，同样是对数级复杂度。
---

## 参考资料

- [二分 - OI Wiki](https://oi-wiki.org/basic/binary/)
- [整数范围二分的正确写法 - bilibili](https://www.bilibili.com/video/BV1np421o77o)

## 二分

**二分**（Binary Search）利用答案的单调性，每次将区间折半，时间复杂度为 $O(\log n)$。

`check` 函数的返回值应为 $\set{0,0,\dots,0,0,1,1,\dots,1,1}$。

二分结束后，$l$ 为第一个 $1$ 的位置，$l-1$ 为最后一个 $0$ 的位置。

<Tabs>
<TabItem value="整数">

```cpp
int l=x,r=y+1;
while(l<r)
{
	int mid=l+r>>1;
	if(check(mid))r=mid;
	else l=mid+1;
}
```

</TabItem>
<TabItem value="实数">

```cpp
double l=x,r=y;
while(r-l>eps)
{
	double mid=(l+r)/2;
	if(check(mid))r=mid;
	else l=mid;
}
```

</TabItem>
</Tabs>

## 三分

**三分**（Ternary Search）用于求单峰（单谷）函数的极值点。每次在区间内取三等分点 $m_1,m_2$，比较 $f(m_1)$ 与 $f(m_2)$ 的大小来排除不含极值的一侧，时间复杂度为 $O(\log n)$。

找最大值时若 $f(m_1)>f(m_2)$，排除右侧；找最小值时若 $f(m_1)<f(m_2)$，排除右侧。

```cpp
double l=x,r=y;
while(r-l>eps)
{
	double m1=(l*2+r)/3,m2=(l+r*2)/3;
	if(f(m1)>f(m2))r=m2;
	else l=m1;
}
```

## 例题

<Problem id="P2249" />

<Problem id="P1883" />

<Problem id="P3382" />

<Problem id="SP4871" />
