---
title: 题解：P8143 [JRKSJ R4] Stirling
description: 本文为洛谷 P8143 的题解，由交换两数使环数奇偶翻转，证明答案为 n 阶乘的一半。
summary: 结论题。对排列对应的置换图,交换其中两个数的位置会让环的数量恰好增加或减少 1——两数同环则该环分裂为二,异环则两环合并为一。由此环数为奇数的排列与为偶数的排列一样多,所求答案为 n 的阶乘的一半。实现上从 3 连乘到 n 并对 998244353 取模即可,时间复杂度 O(n)。
date: 2022-02-12T13:45:28+08:00
authors: lailai
tags: [oi, solution, luogu]
lid: 2cn6fotn
---

{/* truncate */}

## 解题思路

对于任意一个排列的生成图，交换其中两个数的位置，会使环的数量增加或减少 $1$。

证明：交换的两个数如果在同一个环中，这个环会分裂成两个环；如果不在同一个环中，两个环会合并成一个环。

这说明奇数个环和偶数个环的数量相同，因此答案为 $\frac{n!}{2}$。

## 参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

using ll=long long;
const int mod=998244353;
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	ll n;
	cin>>n;
	ll ans=1;
	for(int i=3;i<=n;i++)ans=ans*i%mod;
	cout<<ans<<'\n';
	return 0;
}
```
