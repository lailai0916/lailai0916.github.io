---
title: 题解：P10720 [GESP202406 五级] 小杨的幸运数字
summary: 多组询问，判断给定整数 a 的不同质因子个数是否恰为 2。用试除法遍历 2 到根号 a 的所有因子，每找到一个质因子就把它从 a 中除尽，循环结束后若 a 仍大于 1 则它是唯一大于根号 a 的质因子。统计质因子种数并判断是否等于 2。时间复杂度 O(n 乘根号 a)。
date: 2024-07-09T14:41:32+08:00
authors: lailai
tags: [oi, solution, luogu]
lid: vx1y6eve
---

{/* truncate */}

## 解题思路

遍历整数 $i\in[2,\sqrt{a}]$，检查 $i$ 是否是 $a$ 的因子。如果是，就不断从 $a$ 中去除 $i$ 因子，直到 $a$ 不能被 $i$ 整除为止。

显然 $a$ 最多有 $1$ 个质因子大于 $\sqrt{a}$。循环结束后，如果 $a>1$，那么此时 $a$ 也是一个质因子。

统计质因子的种数，判断其是否等于 $2$。

时间复杂度为 $O(n\sqrt{a})$。

## 参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int n;
	cin>>n;
	while(n--)
	{
		int a;
		cin>>a;
		int cnt=0,t=a;
		for(int i=2;i*i<=t;i++)
		{
			if(a%i==0)
			{
				cnt++;
				while(a%i==0)a/=i;
			}
		}
		if(a>1)cnt++;
		cout<<(cnt==2)<<'\n';
	}
	return 0;
}
```
