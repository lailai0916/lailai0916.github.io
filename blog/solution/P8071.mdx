---
title: '题解：P8071 [COCI 2009/2010 #7] SPAVANAC'
description: 本文为洛谷 P8071 的题解，把分钟减 45，按需退一小时或退到前一天，求早 45 分钟的时刻。
summary: 给定 24 小时制的时刻 H 时 M 分，输出提前 45 分钟的时刻。先把分钟减去 45；若结果为负，则小时减 1、分钟加 60。若小时随后为负，再加 24 回到前一天。两次判断分别处理跨小时与跨日期，最后直接输出调整后的小时和分钟。
date: 2022-01-25T10:50:48+08:00
authors: lailai
tags: [oi, solution, luogu]
lid: dcadtjvw
---

{/* truncate */}

## 题意简述

给定 $24$ 小时制下的时间 $H:M$，输出 $24$ 小时制下比该时间早 $45$ 分钟对应的时刻。

## 解题思路

先直接减去 $45$ 分钟：

$$
M\gets M-45
$$

若 $M<0$，说明要向前退 $1$ 小时：

$$
H\gets H-1,M\gets M+60
$$

若 $H<0$，说明要向前退 $1$ 天：

$$
H\gets H+24
$$

## 参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int h,m;
	cin>>h>>m;
	m-=45;
	if(m<0){h-=1;m+=60;}
	if(h<0)h+=24;
	cout<<h<<' '<<m<<'\n';
	return 0;
}
```
