---
title: 题解：CF1968B Prefiquence
summary: 求字符串 a 的最长前缀，使其为字符串 b 的子序列。贪心地用双指针扫描 b，维护 a 的待匹配位置，当前字符与 a 的待匹配字符相同时该位置右移、前缀长度加一，遍历结束时匹配到的长度即为答案，整体为线性复杂度。
date: 2024-05-04T15:23:15+08:00
authors: lailai
tags: [oi, solution, codeforces]
lid: lfl89s3h
---

{/* truncate */}

## 题意简述

求字符串 $a$ 的最长前缀是字符串 $b$ 的子序列。

## 解题思路

枚举字符串 $b$。如果等于 $a_i$，说明目前长度为 $i$ 的前缀符合要求，后续判断长度为 $i+1$ 的前缀是否符合要求，所以令 $i\gets i+1$。

## 参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int main()
{
	int T;
	cin>>T;
	while(T--)
	{
		int n,m;
		string a,b;
		cin>>n>>m>>a>>b;
		int i=0,j=0;
		while(i<n&&j<m)
		{
			if(a[i]==b[j])i++;
			j++;
		}
		cout<<i<<'\n';
	}
	return 0;
}
```
