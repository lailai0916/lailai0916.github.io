---
title: 题解：CF1899C Yarik and Array
description: 本文为洛谷 CF1899C 的题解，线性遍历求相邻元素奇偶交替的最大子段和，同奇偶时只能另起一段。
summary: 求数列的最大子段和，要求子段内相邻元素的奇偶性交替。从第 2 个元素起遍历，若当前元素与前一个奇偶性不同，则在「接上前面一段」与「另起一段」中取较大者，否则只能另起一段，过程中实时更新最大子段和，一次线性遍历即可完成。
date: 2023-11-19T15:38:08+08:00
authors: lailai
tags: [oi, solution, codeforces]
lid: aya7blkn
---

{/* truncate */}

## 解题思路

对于第 $2\sim n$ 个元素，分类讨论：

1. 若能与前面的元素接上，取接或不接的最大值。
2. 否则，只能选择不接。

实时更新最大值即可。

## 参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N=200005;
int a[N];
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T;
	cin>>T;
	while(T--)
	{
		int n;
		cin>>n;
		for(int i=1;i<=n;i++)cin>>a[i];
		int sum=a[1],ans=a[1];
		for(int i=2;i<=n;i++)
		{
			if(a[i]-a[i-1]&1)sum=max(sum+a[i],a[i]);
			else sum=a[i];
			ans=max(ans,sum);
		}
		cout<<ans<<'\n';
	}
	return 0;
}
```
