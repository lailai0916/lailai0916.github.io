---
title: 题解：P10719 [GESP202406 五级] 黑白格
description: 本文为洛谷 P10719 的题解，枚举子矩形对角，二维前缀和求黑格数，取黑格不少于 k 的最小面积。
summary: n 行 m 列的黑白格矩阵，求黑格数不少于 k 的子矩形的最小面积。数据范围小，O(n 的四次方) 枚举子矩形的左上角与右下角，配合二维前缀和在常数时间内求出每个子矩形内的黑格数量，对满足条件者更新最小面积；若不存在这样的子矩形则输出 0。
date: 2024-07-09T15:50:01+08:00
authors: lailai
tags: [oi, solution, luogu]
lid: yl6wczn4
---

{/* truncate */}

## 解题思路

注意到 $n,m\le 100$，可以 $O(n^4)$ 枚举每个子矩形的左上角 $(x_1,y_1)$ 和右下角 $(x_2,y_2)$。

利用二维 [前缀和](https://oi-wiki.org/basic/prefix-sum/) $O(1)$ 计算每个子矩形中黑色格子数量，维护包含至少 $k$ 个黑色格矩形的面积最小值。

$$
\begin{cases}
  a_{i,j}=b_{i,j}+a_{i-1,j}+a_{i,j-1}-a_{i-1,j-1} \\
  t=\sum_{i=x_1}^{x_2}\sum_{j=y_1}^{y_2}p_{i,j}=a_{x_2,y_2}-a_{x_1-1,y_2}-a_{x_2,y_1-1}+a_{x_1-1,y_1-1} \\
  s=(x_2-x_1+1)(y_2-y_1+1)
\end{cases}
$$

特别地，如果不存在输出 $0$。

## 参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int inf=0x3f3f3f3f;
const int N=105;
int a[N][N];
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int n,m,k;
	cin>>n>>m>>k;
	for(int i=1;i<=n;i++)
	{
		for(int j=1;j<=m;j++)
		{
			char f;
			cin>>f;
			a[i][j]=f-'0'+a[i-1][j]+a[i][j-1]-a[i-1][j-1];
		}
	}
	int ans=inf;
	for(int x1=1;x1<=n;x1++)
	{
		for(int y1=1;y1<=m;y1++)
		{
			for(int x2=x1;x2<=n;x2++)
			{
				for(int y2=y1;y2<=m;y2++)
				{
					int s=a[x2][y2]-a[x1-1][y2]-a[x2][y1-1]+a[x1-1][y1-1];
					if(s>=k)ans=min(ans,(x2-x1+1)*(y2-y1+1));
				}
			}
		}
	}
	cout<<(ans!=inf?ans:0)<<'\n';
	return 0;
}
```
