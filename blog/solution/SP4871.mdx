---
title: 题解：SP4871 BRI - Bridge
description: 本文为洛谷 SP4871 的题解，合并平行两段路径，三分求单峰花费函数的最小值。
summary: 光线以不同单位代价穿过两段平行介质，求从起点到终点的最小总花费。因经过平行介质后方向不变，前后两段路可合并计算：设桥的水平跨度为 x，则桥长为 x 与河宽的平方和开根，剩余路段长为水平 a 加 b、垂直 c 减 x 的平方和开根，总花费 f(x) 是单峰函数，用三分法求其最小值即可。斯涅尔定律因方程两边都带三角函数无法直接求解。
date: 2023-09-28T21:21:36+08:00
authors: lailai
tags: [oi, solution, spoj]
lid: tnebv6sn
---

{/* truncate */}

## 参考资料

- [Snell's law - Wikipedia](https://en.wikipedia.org/wiki/Snell%27s_law)

## 解题思路

根据光学知识，光线通过一段平行介质后与原来平行，所以前后两段路长可以一起计算。

设桥的水平宽度为 $x$，桥的竖直宽度（河的宽度）为 $y$，则桥长为：

$$
\sqrt{x^2+y^2}
$$

剩余部分的水平宽度为 $a+b$，垂直宽度为 $c-x$，则路长为：

$$
\sqrt{(a+b)^2+(c-x)^2}
$$

因此总花费为：

$$
f(x)=s_2\sqrt{x^2+y^2}+s_1\sqrt{(a+b)^2+(c-x)^2}
$$

求导可知 $f(x)$ 是一个单峰函数，可以用 **三分法** 求 $f(x)$ 的最小值。

$$
f'(x)=\frac{s_2x}{\sqrt{x^2+y^2}}+\frac{s_1(x-c)}{\sqrt{(a+b)^2+(c-x)^2}}
$$

我开始考虑用 **斯涅尔定律** 推导公式，但方程两边都带有三角函数，无法直接求解。

## 参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const double eps=1e-10;
int a,b,c,h,s1,s2;
double f(double x)
{
	return s1*sqrt((a+b)*(a+b)+(c-x)*(c-x))+s2*sqrt(h*h+x*x);
}
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T;
	cin>>T;
	while(T--)
	{
		cin>>a>>b>>c>>h>>s1>>s2;
		double l=0,r=c;
		while(r-l>eps)
		{
			double m1=(l*2+r)/3,m2=(l+r*2)/3;
			if(f(m1)<f(m2))r=m2;
			else l=m1;
		}
		cout<<fixed<<setprecision(2)<<f(l)<<'\n';
	}
	return 0;
}
```
