---
title: 题解：P2789 直线交点数
description: 本文为洛谷 P2789 的题解，把平行线组损失的交点转化为完全背包，统计可达成的损失值个数。
summary: 平面上 n 条无三线共点的直线，求可能出现的交点数有多少种。把成组平行线看作损失若干交点，转化为完全背包：设 f[i] 为损失 i 个交点所需的最少直线数，容量为最大损失 n(n-1)/2，物品为平行线组数 j，消耗 j 条直线、损失 j(j-1)/2 个交点。统计能用不超过 n 条直线达成的损失值个数即为答案。时间复杂度为 O(n^3)。
date: 2026-01-06T00:16:50+08:00
authors: lailai
tags: [oi, solution, luogu]
lid: e54rbro6
---

{/* truncate */}

## 参考资料

- [A069999 - OEIS](https://oeis.org/A069999)

## 题意简述

平面上有 $n$ 条直线，且无三线共点，求这些直线有多少种可能的交点数。

## 解题思路

此问题可以转化为 **完全背包** 问题：

- 状态定义：$f_i$ 表示损失 $i$ 个交点消耗直线数量的最小值；
- 背包容量：最大交点损失数量 $m=\frac{n(n-1)}{2}$；
- 物品类型：平行线组数量 $j\in[2,n]$；
- 物品价值：消耗直线数量 $j$；
- 物品体积：交点损失数量 $w=\frac{j(j-1)}{2}$；
- 状态转移：$f_i=\min(f_i,f_{i-w}+j)$。

时间复杂度为 $O(n^3)$。

## 参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int inf=0x3f3f3f3f;
const int N=1005;
int f[N];
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int n;
	cin>>n;
	int m=n*(n-1)/2;
	for(int i=1;i<=m;i++)f[i]=inf;
	for(int j=2;j<=n;j++)
	{
		int w=j*(j-1)/2;
		for(int i=w;i<=m;i++)f[i]=min(f[i],f[i-w]+j);
	}
	int ans=0;
	for(int i=0;i<=m;i++)if(f[i]<=n)ans++;
	cout<<ans<<'\n';
	return 0;
}
```
